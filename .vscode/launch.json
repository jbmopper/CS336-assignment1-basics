{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Decode (module)",
            "type": "debugpy",
            "request": "launch",
            "module": "cs336_basics.decode",
            "cwd": "${workspaceFolder}",
            "args": [
                "${workspaceFolder}/checkpoints/latest.pt"
            ],
            "console": "integratedTerminal",
            "justMyCode": false,
            "env": {
                "PYTHONPATH": "${workspaceFolder}"
            }
        }
    ]
}